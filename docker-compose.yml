services:
  app:
    build: .
    command: sh -c "npm ci && npm run dev -- --host 0.0.0.0"
    ports:
      - '5173:5173'
    volumes:
      # ソースコードをマウント（ホットリロード用）
      - .:/app
      # node_modules はコンテナ内のものを使う（ホスト側を上書きしない）
      - /app/node_modules
    environment:
      - NODE_ENV=development
